\chapter{Grothendieck Topology}

In this chapter, we introduce Grothendieck topologies on categories as a generalization of classical topologies on spaces. Given a topological space 
$X$, its open subsets form a category $Open(X)$, whose objects are the open sets $U\subset X$ and whose morphisms are inclusions $V\hookrightarrow U$. Sheaves on $X$ can be viewed equivalently as certain contravariant functors on this category that satisfy a gluing condition with respect to open coverings. In this way, the usual notion of “covering an open subset $U$ by smaller opens $\{U_i\subset U\}$" is encoded purely in terms of the category $Open(X)$.

Grothendieck’s idea is to abstract this picture: Instead of restricting to open subsets and inclusion maps, one chooses a category $\CatC$ and specifies, for each object $U\in \CatC$, which families of morphisms  $\{U_i \to U\}$ should be considered  as "coverings”. These covering families are required to satisfy axioms modeled on the behavior of open covers in classical topology, and the resulting structure is called a Grothendieck topology on $\CatC$.

By this approach, one can treat, for example, coverings of schemes (such as \etale\ or fppf covers) and coverings of rings (via their associated affine schemes) within the same framework.

Conceptually, this follows the general Yoneda philosophy: A space (or more generally, an object) can be studied and reconstructed from its functor points, i.e. the morphisms into it. Grothendieck topologies implement this idea by replacing open subsets of a space with families of morphisms in an arbitrary category that behave like covers, and then defining sheaves as functors on $\CatC$ that satisfy a suitable descent condition with respect to these covering families.

\begin{definition}[Sieve]
	(\cite[][p.~38]{MacLaneMoerdijk1992})
	Let $X\in \CatC$ be an object of a small category. A sieve on $X$ is a subfunctor of the representable presheaf $\Hom_\CatC(-, X)$. We will write $h_X$ for the Yoneda embedding $\Hom_\CatC(-, X)$.
\end{definition}

\begin{definition}[Grothendieck Topology]
	(\cite[(Definition 1), p. ~110]{MacLaneMoerdijk1992})
	A Grothendieck topology $J$ on a small categrory $\CatC$ is a function that assigns to each object $X\in C$ a collection of sieves $J(X)$ called covering sieves, such that the following axioms are satisfied:
	\begin{enumerate}
		\item For each object $X\in \CatC$, $\Hom_\CatC(-, X)$ is a sieve.
		\item For each morphism $f:X\to Y$ in $\CatC$ and each sieve $S\in J(Y)$ the pullback $f^*(S)\in J(X)$ is a sieve on $X$.
		\item For each covering sieve $S\in J(Y)$ and each sieve $R$ on $Y$, such that $f^*(R)\in J(X)$ for all $f:X\to Y\in S$, then $R\in J(Y)$
	\end{enumerate}
\end{definition}

\begin{remark}
	For a sieve $S$ on $Y\in\CatC$ and any $f:X\to Y$ in $\CatC$ we have $f^*S = \{g:Z\to X\mid f\circ g\in S\}\subset \Hom_\CatC(-, X)$
\end{remark}

\begin{definition}[Site]
	(\cite[][p. ~110]{MacLaneMoerdijk1992})
	For a small category $\CatC$ and a Grothendieck topology $J$ we will call the pair $(\CatC, J)$  a site on $\CatC$.
\end{definition}

\begin{definition}[Sheaf on a cite]\label{def:sheaf-on-cite}
	(\cite[][Tag 00VM]{stacks-project})
	Let $\CatC$ be a site which is closed under pullbacks. We call a presheaf $F$ on $\CatC$ a sheaf, if for every covering $\{U_i\to X\}_{i}$ the following diagramm is an equalizer:
	% https://q.uiver.app/#q=WzAsMyxbMCwwLCJGKFUpIl0sWzEsMCwiXFxwcm9kX2lGKFVfaSkiXSxbMiwwLCJcXHByb2Rfe2ksan1GKFVfaVxcdGltZXNfVSBVX2opIl0sWzAsMV0sWzEsMiwiXFxwaV8xXioiLDIseyJvZmZzZXQiOjF9XSxbMSwyLCJcXHBpXzBeKiIsMCx7Im9mZnNldCI6LTF9XV0=
	\begin{equation}\label{eq:sheaf-condition-equalizer}
		\begin{tikzcd}
			{F(X)} & {\prod_iF(U_i)} & {\prod_{i,j}F(U_i\times_X U_j)}
			\arrow[from=1-1, to=1-2]
			\arrow["{\pi_1^*}"', shift right, from=1-2, to=1-3]
			\arrow["{\pi_0^*}", shift left, from=1-2, to=1-3]
		\end{tikzcd}
	\end{equation}
\end{definition}

\begin{lemma}\label{lem:sheaf-preserves-coproducts}
	Let $F$ be a sheaf on a cite $(\CatC,J)$. Then
	\[
	F\big(\coprod_i U_i\big) \cong \prod_i F(U_i).
	\]
\end{lemma}

\begin{lemma}[Single-cover reformulation of the sheaf condition]\label{lem:sheaf-single-cover}
	Let $(\CatC, J)$ be a site and let $F$ be a presheaf on $\CatC$. Let $\{U_i \to X\}_i$ be a covering and set
	\[
	U := \coprod_i U_i .
	\]
	Suppose $F$ satisfies $\prod_i F(U_i) \cong F(U)$ compatibly with the natural inclusion-induced maps (i.e. Lemma~\ref{lem:sheaf-preserves-coproducts} holds for $U$). Then $F$ satisfies the sheaf condition of Definition~\ref{def:sheaf-on-cite} for the covering $\{U_i \to X\}_i$ if and only if the diagram
	\[
	F(X) \longrightarrow F(U) \rightrightarrows F(U \times_X U)
	\]
	is an equalizer, where the two parallel arrows are induced by the two projections $U \times_X U \rightrightarrows U$.
\end{lemma}

\begin{proof}
	By hypothesis and the distributivity of fiber products over coproducts,
	\[
	U \times_X U
	=
	\Bigl(\coprod_i U_i\Bigr) \times_X \Bigl(\coprod_j U_j\Bigr)
	\cong
	\coprod_{i,j} (U_i \times_X U_j),
	\]
	so we have canonical isomorphisms
	\[
	F(U) \cong \prod_i F(U_i),
	\qquad
	F(U \times_X U) \cong \prod_{i,j} F(U_i \times_X U_j).
	\]
	Under these identifications, the two projections $\pi_1, \pi_2 : U \times_X U \to U$ restrict on the summand $U_i \times_X U_j$ to the two projections $U_i \times_X U_j \to U_i$ and $U_i \times_X U_j \to U_j$. Hence, componentwise in $(i,j)$, the induced maps
	\[
	F(U) \rightrightarrows F(U \times_X U)
	\]
	agree with
	\[
	\pi_0^*, \pi_1^* : \prod_i F(U_i) \rightrightarrows \prod_{i,j} F(U_i \times_X U_j)
	\]
	from Definition~\ref{def:sheaf-on-cite}. Since these identifications are natural isomorphisms of diagrams commuting with the structure map from $F(X)$, the two equalizer diagrams are isomorphic as diagrams, and being an equalizer is invariant under such an isomorphism.
\end{proof}

\begin{remark}
	The single-cover reformulation of Lemma~\ref{lem:sheaf-single-cover} is not just a notational convenience, but it identifies the sheaf condition with the equalizer of the first two stages of the Čech nerve
	\[
	U_\bullet \colon
	U
	\;\substack{\longrightarrow\\[-0.4em]\longrightarrow}\;
	U \times_X U
	\;\substack{\longrightarrow\\[-0.4em]\longrightarrow\\[-0.4em]\longrightarrow}\;
	U \times_X U \times_X U
	\;\cdots
	\]
	associated to the single morphism $U := \coprod_i U_i \to X$, as introduced in
	Chapter \ref{chap:descent-theory}. Working with a single cover $U \to X$, rather than an indexed family $\{U_i \to X\}_i$, allows us to treat sheaf and descent conditions uniformly as statements about the cosimplicial diagram $F(U_\bullet)$, without carrying along the extra indices $i,j,k$ in each fiber product. This simplification becomes essential once we pass to presheaves valued in $\Grpd$ or $\Cat$ in Chapter \ref{chap:descent-theory}, where descent data and the cocycle condition are most naturally phrased in terms of the simplicial structure of $U_\bullet$ rather than in terms of an explicit covering family.
\end{remark}

\begin{lemma}[Sheafication]\label{lem:sheafification}
	(\cite[p. 134]{MacLaneMoerdijk1992})
	Let $(\CatC, J)$ be a site, then the fully faithful inclusion $\iota: \Sh{\CatC,J} \hookrightarrow \PshC$ admits a left adjoint $\adj{(-)^\sharp}{\iota}$.
	We write $\eta^2: F \to F^\sharp$ for the unit evaluated at a presheaf $F$.
\end{lemma}

\begin{corollary}\label{cor:limit-sheaf}
	By \cite[]{MacLaneCFWM}, the category $\Sh{\CatC,J}$ is complete. In particular the limit of sheaves is again a sheaf and can be calculated in the  category $\PshC$ of presheaves.
\end{corollary}

